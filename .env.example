# 环境变量模板（.env.example）- 不含真实密码
PORT=3000

# 客户端凭据（运行时生成，非住宅密码）
CLIENT_UUID=<CLIENT_UUID>
WS_PATH=<WS_PATH>
SUB_TOKEN=<SUB_TOKEN>

# 住宅代理（密码必须放 DCDeploy Secret，不放这里）
RES_PROXY_HOST=63.127.61.176
RES_PROXY_PORT=6894
RES_PROXY_USER=eduvavxu
RES_PROXY_PASS=<RES_PROXY_PASS_SECRET_ONLY>
RES_PROXY_TYPE=socks

# 公网域名（DCDeploy 部署后填充）
PUBLIC_HOST=<service>.dcdeploy.cloud
